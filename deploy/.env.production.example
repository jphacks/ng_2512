# Recall Production Runtime Environment
# Use this file as the baseline when provisioning secrets (1Password, Vault, etc.).
# Values containing PLACEHOLDER_... must be replaced before going live.

# --- Core service configuration ---
FLASK_ENV=production
SECRET_KEY=PLACEHOLDER_GENERATE_STRONG_SECRET
LOG_LEVEL=INFO

# --- Network / external endpoints ---
PRIMARY_DOMAIN=recall.example.com
PUBLIC_BASE_URL=https://recall.example.com

# --- Data stores ---
DATABASE_URL=postgresql+psycopg2://PLACEHOLDER_DB_USER:PLACEHOLDER_DB_PASS@db:5432/recall
REDIS_URL=redis://redis:6379/0
VLLM_ENDPOINT=https://llm.example.com/v1

# --- Feature flags / integrations ---
SENTRY_DSN=
OTLP_ENDPOINT=
FEATURE_DEMO_MODE=false

# --- Deployment metadata (mirrors deploy/.env.deploy.example) ---
IMAGE_REPO=ghcr.io/your-org/recall/api
IMAGE_TAG=sha-yourcommit
COMPOSE_PROFILES=postgres,redis,vllm
HEALTHCHECK_URL=https://recall.example.com/readyz
HEALTHCHECK_TIMEOUT=90
MIGRATION_COMMAND=flask db upgrade
